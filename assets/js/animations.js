(()=>{'use strict';addEventListener('load',()=>setTimeout(()=>document.querySelector('.loader')?.classList.add('done'),250));const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,items=document.querySelectorAll('.reveal');if(reduced){items.forEach(x=>x.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -30px'});items.forEach((x,i)=>{x.style.transitionDelay=`${Math.min((i%3)*.09,.18)}s`;observer.observe(x)});if(matchMedia('(pointer:fine)').matches)document.querySelectorAll('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*4}deg) rotateY(${x*4}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')})})();
